[tool.poetry]
name = "DDataFlow"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
databricks-cli = {version = "^0.16.6"}
pyspark = "^3.2.1"

[tool.poetry.scripts]
mltools  = 'ddataflow_cli'

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
